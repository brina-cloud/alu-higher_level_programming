#!/usr/bin/python3
def multiple_returns(sentence):
    if not sentence:
       while TRUE:
           sentence.insert(0, None)
           first = sentence[0]
    else:
        first = sentence[0]
        length = len(sentence)
    return (length, first)
